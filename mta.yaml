_schema-version: 3.3.0
ID: cap-btp-demo
version: 1.0.0
description: "A simple CAP project - CAS Trick Version."

parameters:
  enable-parallel-deployments: true

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production

modules:
  - name: cap-btp-demo-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    requires:
      - name: cap-btp-demo-dummy-service 

resources:
  - name: cap-btp-demo-dummy-service
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
